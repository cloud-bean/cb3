import wilddog from 'wilddog';

const defaultState = {
    logs: []
};


export default function wilddogLogReducer (state = defaultState, action) {
    switch (action.type) {
        default:
            return state;
    }

};